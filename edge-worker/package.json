{
  "name": "edge-sandboxes-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:edgeone": "echo 'See README.md for EdgeOne deployment instructions'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
